///using string as subscript in vector and push string in the vector
map<string,vector<string> >m;
/// using two strings as subscript in 2D array
map<string, map<string, ll> >cost;
void resize (ll n);///n = New string length, expressed in number of characters.
void resize (ll n, char c);///c=Character used to fill the new character space added to the string
string& insert (ll pos, const string& str, ll subpos, ll sublen);
find(string)///returns string::npos if not found
string str2 = str.substr (3,5); ///from position 3 , 5 characters
str.erase (10,8); ///10th pos thek 8 ta letter erase
getline(cin,name);
__builtin_popcount(x);
__builtin_popcountll(x);
